## How to test an E2E test locally

I'm launching a second instance locally. In the parent instance, I need to change the appsettings in the project containing E2E tests so that the API connection configuration points to localhost:port - and that's basically it 🙂

## E2E tests in the cloud

On the environments, there are always instances of different services up so that E2E tests can easily reach them.
